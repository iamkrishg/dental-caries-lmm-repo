# Results

`summary.json` — aggregate metrics from the reported evaluation run
(232 images, test split, zero-shot, single pass).

Per-image results (`results.json`) and overlay images are gitignored by
default because they contain large volumes of raw model output. Remove the
relevant lines from `.gitignore` if you wish to publish them.
